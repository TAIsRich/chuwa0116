1. Learn MarkDown and show all of basic usage in the ShortQuestions/README.md
    - https://docs.github.com/en/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax
2. practicegitusingtheplatform.listthegitcommandsyoulearned
    git branch
    git merge
    git rebase
    git log
    git stash
    git reset
3. What is the basic steps to init a git repo in you local

git init
git add .
git commit -m "commit message"
git remote add origin https://github.com/USER/REPO.git
git push -u origin master


4.How to clone a repo from Github ?
navigate to the main page of the repository and find the url on that page
navigate to a directory in local
run the command 'git clone https://github.com/USER/REPO.git'

5.How to create a new branch and checkout to that branch ?

git branch [new_branch_name]
git checkout [new_branch_name]
or
git checkout -b [new_branch_name]

6. How to merge the branch_test to master branch in command ? show me the commands

git checkout master
git pull
git merge branch_test
git commit
git push

7. How to stash your new code before leaving branch branch_learn_stash and pop your stash when you checkout back to branch_learn_stash ? try commands way and intellij way.

git stash save "branch_learn_stash"
git checkout branch_learn_stash
git stash pop


8. How do you understand PR is based on Branch?
    Pull requests initiate the process of integrating new code changes into the main project repository.
    Pull requests notify the rest of your team that a branch or fork is ready to be reviewed.

9. What is maven role ? what it be used to do ?
    Maven is a project management tool.
    Maven is used to build and manage any Java-based project.
10. What is the lifecycle of maven? could you tell me the details
    Lifecycle of Maven includes Validate, Compile, Test, Package, Integration test, Verify, Install and Deploy.
    Validate: This step validates if the project structure is correct.
    Compile: It compiles the source code, converts the .java files to .class and stores the classes in target/classes folder.
    Test: It runs unit tests for the project.
    Package: This step packages the compiled code in distributable format like JAR or WAR.
    Integration test: It runs the integration tests for the project.
    Verify: This step runs checks to verify that the project is valid and meets the quality standards.
    Install: This step installs the packaged code to the local Maven repository.
    Deploy: It copies the packaged code to the remote repository for sharing it with other developers.

11. what is the difference between package and install in maven lifecycle
     In Package phase Maven packages the compiled code in distributable format like JAR or WAR.
     Then, in Install phase Maven installs these package in local/remote maven repository

12. What is plugins in maven, list some plugins.
    Plugins allow for the reuse of common build logic across multiple projects.
    Maven Plugins are used to create jar file. create war file.
    Maven plugins: clean
                   deploy
                   install
                   resources
                   jar
